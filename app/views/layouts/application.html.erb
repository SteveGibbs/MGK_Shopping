<!DOCTYPE html>
    </html>

    <head>
        <title>MgkShoppingApp</title>
        <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
        <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
        <%= csrf_meta_tags %>
        <link href="//fonts.googleapis.com/css?family=Lato" rel="stylesheet">
        <link rel="stylesheet" type="text/css" href="//cdn.jsdelivr.net/jquery.slick/1.6.0/slick.css"/>
        <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/font-awesome/4.6.3/css/font-awesome.min.css">
    </head>
    <body>
        <header>
            <div class="row">
                <div class="col-12-sm center">
                    <h1>
                        <i class="fa fa-stop-circle" aria-hidden="true"></i>
                        MGK SYDNEY</h1>
                </div>
            </div>

            <div class="row">
                <nav>
                    <%= link_to("Browse", products_path) %>

                    <!-- Admin view -->
                    <% if @current_user %>
                    <br>
                    <%= link_to("New Product", new_product_path) %>
                    |
                    <%= link_to("All Orders", orders_path) %>
                    |
                    <%= link_to("Competitors", competitor_path) %>
                    <% end %>
                    <!-- Admin view -->
                    <br>

                    <% unless @current_user %>
                    <% if @current_order %>
                    <%= link_to("Current order: #{@current_order.status}", order_path(@current_order)) %>
                    <i class="fa fa-credit-card-alt" aria-hidden="true"></i>
                <% else %>
                    <%= link_to("Cart: #{@current_cart.total_quantity}",cart_path(@current_cart)) %>

                    <i class="fa fa-shopping-cart" aria-hidden="true"></i>
                    <%=  %>
                    <% end %>
                    <% end %>

                    <% if @current_user %>
                    <p>Logged in :
                        <%= @current_user.first_name %>
                        (<%= link_to "Logout", logout_path, method: :delete %>)</p>
                    <% end %>
                </nav>
            </div>
        </header>

        <div class="container">
            <%= yield %>
        </div>

        <footer>

            <div class="row">
                <div class="col-6">
                    <h4>About Us</h4>
                    <small>MGK watches are created for free-spirited individuals. MGK makes fashion-forward watches where classic meets modern. Where function meets form. Where Amsterdam meets New York.</small>

                    <% if @current_user %>
                    <p>Logged in :
                        <%= @current_user.first_name %>
                        (<%= link_to "Logout", logout_path, method: :delete %>)</p>
                <% else %>
                    <p><%= link_to("Admin login", login_path) %></p>
                    <% end %>

                </div>
                <div class="col-6">
                    <h4>Project Summary</h4>
                    <p>Group project as part of General Assembly's Web Development Immersive bootcamp. Project inspiration from e-commerce watch store
                        <a href="https://www.rosefieldwatches.com/" target="_blank">Rosefields</a>.

                        <p>Built by project team in four days. Find our other work on github:</p>
                        <ul class="fa-ul">
                            <li>
                                <i class="fa fa-github-alt" aria-hidden="true"></i>
                                <a href="https://github.com/SteveGibbs" target="_blank">Steve Gibbs</a>
                            </li>
                            <li>
                                <i class="fa fa-github-alt" aria-hidden="true"></i>
                                <a href="https://github.com/skal9606" target="_blank">Samit Kalra</a>
                            </li>
                            <li>
                                <i class="fa fa-github-alt" aria-hidden="true"></i>
                                <a href="https://github.com/howardmann" target="_blank">Howie_Burger</a>
                            </li>
                        </ul>

                    </div>
                </div>
            </footer>

            <script type="text/javascript" src="//cdn.jsdelivr.net/jquery.slick/1.6.0/slick.min.js"></script>
        </body>

    </html>
